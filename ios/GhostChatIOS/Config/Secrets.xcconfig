// Define o segredo em Secrets.local.xcconfig (gitignored) — usado sobretudo se usares Info.plist manual.
// O método recomendado para TestFlight é preencher `GhostChatSecrets.swift` (mesmo valor que na Vercel).
GHOSTCHAT_IOS_API_SECRET =
#include? "Secrets.local.xcconfig"
